const http=require('http'),fs=require('fs'),path=require('path');const port=process.env.PORT||3000;http.createServer((req,res)=>{let p=req.url==='/'?'/index.html':req.url;p=path.join(__dirname,p);fs.readFile(p,(e,d)=>{if(e){res.writeHead(404);return res.end('Not found')}res.writeHead(200,{'Content-Type':p.endsWith('.html')?'text/html':'text/plain'});res.end(d)})}).listen(port,'0.0.0.0',()=>console.log('Architecture Studio on '+port));